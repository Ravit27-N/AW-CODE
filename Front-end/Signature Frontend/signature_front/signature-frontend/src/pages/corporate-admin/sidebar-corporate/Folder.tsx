import {Center, VStack} from '@/theme';
import NGText from '@components/ng-text/NGText';

const FolderCorporate = () => {
  return (
    <Center sx={{height: '100vh'}}>
      <VStack>
        <NGText text={'My Folder'} />
      </VStack>
    </Center>
  );
};
export default FolderCorporate;
